spring:
  application:
    name: crypto-service-unit-test

  datasource:
    url: jdbc:h2:mem:unittest;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop

crypto:
  aes:
    key-length: 256
  max-tries: 3
